{
  "packageManager": "npm@11.0.0",
  "name": "nextelements",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "push": "node ./scripts/push-to-git.js"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "@blueprintjs/icons": "^5.16.0"
  }
}
