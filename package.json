{
  "packageManager": "npm@11.0.0",
  "name": "nextelements",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "push": "sh ./scripts/push.sh"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
