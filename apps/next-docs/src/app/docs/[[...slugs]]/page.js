import { createStructure } from '@/lib/structure'

export default () => {

  const items = createStructure()

  //console.log(JSON.stringify(items, null, 1))

  // look at navigation-project
  return 'Slug Page'

}