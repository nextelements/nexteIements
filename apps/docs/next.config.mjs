/** @type {import('next').NextConfig} */

const nextConfig = {
  transpilePackages: [ 
    '@nextelements/components',
    '@nextelements/icons',
    '@nextelements/themes',
    '@nextelements/utilities',
  ],
}

export default nextConfig;
